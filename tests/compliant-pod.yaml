apiVersion: v1
kind: Pod
metadata:
  name: compliant-pod
spec:
  containers:
    - name: nginx
      image: nginx:1.27
      securityContext:
        privileged: false
      resources:
        limits:
          memory: "128Mi"
          cpu: "250m"